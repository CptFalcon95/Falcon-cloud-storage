<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <a class="navbar-brand" href="#">
        <img src="/images/logo_eagle_w.png" width="30" height="30" class="d-inline-block align-top" alt="">
        Falcon Cloud <span class="text-muted">Storage</span>
    </a>
    <div class="ml-auto">
        <% if (page == "login") { %>
            <a href="/user/register" class="btn btn-secondary">Register</a>
        <% } %>
        <% if (page == "register") { %>
            <a href="/user/login" class="btn btn-green text-white">Login</a>
        <% } %>       
    </div>
</nav>